Plan to finish DashboardScreen.kt rewrite

1) Copy the full rewritten DashboardScreen implementation into this file for safe review.
2) Then overwrite DashboardScreen.kt with the exact contents (tool timeout previously blocked full overwrite).
3) Validate compilation (ktlint / Gradle build if wrapper works).

Note: In this environment, direct full-file overwrite of DashboardScreen.kt timed out.

